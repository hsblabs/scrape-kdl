# @hsblabs/scrape-kdl-playwright

Reserved package boundary for the official Playwright adapter.

The package is private until Issue #16 implements and verifies the adapter.
It intentionally contains no Playwright dependency at the scaffold milestone, and the core `@hsblabs/scrape-kdl` package never depends on Playwright.

License: Apache-2.0.
